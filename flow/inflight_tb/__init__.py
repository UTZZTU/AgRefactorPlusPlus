"""Testbench checks used by the testbench-coverage / hidden-TB optimizer.

`checks.py` provides self-contained helpers (e.g. extracting the `<kernel>_hls`
declaration from a testbench, signature linting) consumed by
`flow.tools.tb_optimizer`. No external dependencies.
"""
