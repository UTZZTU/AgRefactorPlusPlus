# P0 Prompt Identity Reconciliation

## Observed real-run evidence

A real DFS source-only run on commit
`8dfcffef687acf8aab9643e2a8ec1b1b4c2c1644` completed successfully:

```text
Status: accepted
CSYNTH: passed
Public tests: passed
Hidden tests: passed
Public Testbench repairs: 1
Hidden Testbench exposed to model: false
```

The shared budget correctly recorded 30 LLM calls. Prompt Identity
contained the 29 Legacy/AG2 calls but omitted the one provider-backed
Public Testbench repair call, even though its prompt SHA and cost were
retained elsewhere.

## Correction

Public Testbench preparation now converts every retained repair
`audit_event` into the same safe Prompt Identity record used by other
model calls. Only template identifiers, call metadata and SHA-256
hashes are retained; plaintext prompts and Hidden Testbench content
remain excluded. Multiple repair attempts are represented separately.

This correction closes only the observed evidence-accounting gap.
A new real P0 run is still required because the prior Execution
Identity was generated by the pre-correction commit.

```text
P0_STATUS=active_retry_required
PRE_STAGE3_CLOSED=false
STAGE3_STARTED=false
```
